import React from "react";

const Total = (props) => {

    return (
      <div>
        <p>Number of exercises {props.exercise + props.exercises + props.exercise}</p>
      </div>
    )
  }

export default Total;